#! /usr/bin/env python

from os.path import join
import os
import glob

# list of all VVDS spectra
stackList = glob.glob(join(os.environ['SPECTRASTACKS_DIR'],"data","??_????","*VVDS*R_*.fits"))

for el in stackList:
	os.system("python stellarpop_stacks "+el)

	